# iq

A lightweight command-line tool for managing and querying your personal knowledge base from the terminal.

- Index Markdown notes, PDFs, and code snippets into a searchable local store
- Natural language query interface powered by local LLM embeddings
- Tag-based organization with smart auto-tagging suggestions
- Fast fuzzy search across all content with relevance ranking
- Export results to Markdown or JSON for integration with other tools
